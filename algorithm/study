# 알고리즘 : 문제를 푸는 방법이나 절차
어떤 특징, 계산이 얼마나 빠르고 편한지 등을 고려하여 선택

# 빅-오 표기법 : 계산 복잡도 / 가장 길게 걸리는 시간 표현
O(n-1)로 표현하지 않고 O(n)으로 표현함

# 리스트 함수 / 중복 가능
1. len(리스트) : 길이
2. 리스트.apppend(요소) : 요소 추가
3. 리스트.insert(idx, 요소) : idx 위치에 요소 추가
4. 리스트.pop() : 가장 마지막 요소 삭제 후 출력
5. 리스트.clear() : 리스트 비우기
6. 요소 in 리스트 : 요소가 리스트에 있는지 확인

# set(집합) 함수 / 중복 불가능
1. len(집합) : 길이
2. 집합.add(요소) : 요소 추가 (중복이면 추가 안됨)
3. 집합.discard(요소) : 요소 삭제
4. 집합.clear() : 집합 비우기
5. 요소 in 집합 : 요소가 집합에 있는지 확인